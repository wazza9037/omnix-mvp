# Development-only dependencies. Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

# Testing
pytest>=7.4,<9.0
pytest-cov>=4.1,<6.0

# Linting & formatting (optional but recommended)
ruff>=0.1,<1.0

# Pre-commit hooks
pre-commit>=3.5,<4.0
